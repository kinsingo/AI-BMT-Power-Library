# Zeus Power Monitor - Python Dependencies
#
# Install: pip install -r requirements.txt
# Usage:   python main.py
#
# Core: At least one GPU/device backend is needed.

# NVIDIA GPU (NVML bindings) - most common
pynvml>=11.5.0

# Optional backends (uncomment or install separately as needed):
# amdsmi                # AMD GPU (requires ROCm >= 6.1)
# zeus-apple-silicon    # Apple Silicon SoC energy measurement
